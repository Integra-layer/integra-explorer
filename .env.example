# Vue Admin (root) — Environment Variables
# Copy to .env and fill in values.

# Soketi / WebSocket (for real-time updates in admin)
VITE_SOKETI_HOST=localhost
VITE_SOKETI_PORT=6001
VITE_SOKETI_KEY=app-key
